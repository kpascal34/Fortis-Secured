// Demo audit logs are intentionally empty in production builds.
// Populate from your data source if you need seeded records for non-prod environments.
export const demoAuditLogs = [];

export default demoAuditLogs;
