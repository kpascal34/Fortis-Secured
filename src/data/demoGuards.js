// Demo guards are intentionally empty in production builds.
// Populate from your data source if you need seeded records for non-prod environments.
export const demoGuards = [];

// Active guards only (for assignment modals)
export const activeDemoGuards = demoGuards;
